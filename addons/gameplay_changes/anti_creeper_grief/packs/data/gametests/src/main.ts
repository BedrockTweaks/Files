// import { system, TicksPerSecond } from '@minecraft/server';
import './Events';

/**
 * Entry point of the addon
 *
 * We do 2 things here
 * Import the Events module
 * Execute any system functions which need to be constantly running
 */

// system.runInterval((): void => {
// 	// Action to run on an interval
// }, TicksPerSecond);
