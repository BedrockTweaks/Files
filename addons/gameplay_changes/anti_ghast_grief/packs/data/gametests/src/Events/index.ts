/**
 * Events Module
 *
 * This module imports and registers all event handlers.
 * Events that have both "before" and "after" versions are organized within the same file.
 */
import './explosion';
import './scriptEventCommandMessage';
// import './worldInitialize'; // disabled to not bundle all that code into final build since it is unused.
